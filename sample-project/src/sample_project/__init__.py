"""sample-project — a fixture for oxi end-to-end smoke tests.

This package exists only so that the reference oxi adapter has a real
Python project to drive. The roadmap at `../roadmap.md` enumerates the
work items the adapter will ask oxi to ship.
"""

__version__ = "0.0.0"
