"""Trivial module used as a change target for smoke tests.

The roadmap item T0-1 ("add a greet function") will eventually land a
function here. For now the module contains a single sentinel constant
so tests have something to assert against.
"""

SENTINEL = "sample-project"
