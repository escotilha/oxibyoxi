# `oxi init` wizard

**Status:** stub. Phase 3 deliverable.

The 8-step bootstrap flow a new user runs after `pip install oxi-core`. Each step is documented once the wizard is implemented and has been user-tested against a throwaway project.
